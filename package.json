{
  "name": "third-av",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "watchify index.js -o bundle.js & serve -p 1315",
    "build": "NODE_ENV=production browserify index.js | uglifyjs -c -m > bundle.js"
  },
  "repository": "",
  "author": "",
  "license": "MIT",
  "browserify": {
    "transform": [
      "babelify", "brfs"
    ]
  },
  "devDependencies": {
    "babelify": "^5.0.3",
    "browserify": "^8.1.0",
    "uglify-js": "^2.3.6",
    "watchify": "^2.2.1",
    "sluggo": "^0.2.0"
  },
  "dependencies": {
    "when": "^3.6.4",
    "react-router": "^0.11.6",
    "react": "^0.12.2"
  }
}
